image: Visual Studio 2017

environment:
  matrix:
    - nodejs_version: ""
    - nodejs_version: "7"

matrix:
  fast_finish: true

platform:
  - x64

clone_depth: 5

install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - yarn install

test_script:
  - node --version
  - npm --version
  - yarn --version
  - yarn test

cache:
 - node_modules
 - "%LOCALAPPDATA%/Yarn"
